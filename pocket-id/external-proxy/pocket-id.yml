---
services:
  pocket-id:
    ports:
      # When cloudflared runs as a Docker container, use the host LAN IP
      # so it can reach Pocket ID. Set HOST_IP in .env.
      # When cloudflared runs as a system service, 127.0.0.1 is sufficient.
      - "${HOST_IP:-127.0.0.1}:1411:1411"
